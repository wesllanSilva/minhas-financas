[pytest]
testpaths = testes
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning

# O Streamlit avisa "missing ScriptRunContext" toda vez que o `core` é chamado
# fora de um app rodando — que é exatamente como os testes o exercitam.
log_cli = false
